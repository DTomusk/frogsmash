import { AppBar, Toolbar } from "@mui/material";

function NavBar() {
    return (<AppBar position="static">
        <Toolbar>
            Frog Smash
        </Toolbar>
    </AppBar>);
}

export default NavBar;