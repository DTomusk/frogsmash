# ---------- Dockerfile.dev ----------
FROM node:25-slim

WORKDIR /app

# Copy dependency manifests
COPY package*.json ./

# Install dependencies (skip build)
RUN npm ci

# Expose Vite’s default dev port
EXPOSE 5173

# Default command for dev mode
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]